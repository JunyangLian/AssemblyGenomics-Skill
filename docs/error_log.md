# 错误日志

记录失败现象、根因证据、修复与复测。当前开发阶段侧重开发期问题与测试失败。

| 时间 | 现象 | 根因 | 修复 | 复测 |
|---|---|---|---|---|
| 2026-09-22 | 用户在服务器执行 PIT-005 检查命令报 awk syntax error（unexpected newline） | 会话中手打的单行 awk 压缩时丢失一个右括号；仓库 YAML 里的多行版本本身正确（本地合成测试通过） | 改发与仓库 YAML 完全一致的多行版本；教训：给用户的命令必须从已测试源提取，不得手打改写 | 服务器重跑：47848/23924 与 23924/23924，阳性阴性均正确 |
| 2026-09-22 | PIT-001 famdb 探测无输出 | shell 管道 `cmd \| head \|\| echo` 的退出码取自 head（恒 0），失败分支被吞；且 famdb 需用其所属 env 的 python 调用 | 先 `ls -d` 定位 famdb.py，用 env 内 python 执行，不用管道吞退出码 | Dfam 4.0 正常回显（末尾 BrokenPipeError 为 head 关闭管道的无害噪音） |
| 2026-09-22 | 酵母数据 md5 校验大面积 "FAILED open or read"（用户服务器） | NCBI md5checksums.txt 覆盖整个 release 目录，但只下载了 4 个文件——未下载文件报"不存在"属预期噪音，指令里的"应无 FAIL"表述误导；实际 4 个已下载文件全部校验通过 | 改为只对已下载文件清单执行 `grep -E ... \| md5sum -c -`；教训：给用户的校验命令必须与实际下载清单对齐 | 修正命令复测：3 个数据文件 OK |
| 2026-09-22 | UniProt `uniprot_sprot.fasta.gz.md5` 404 | 该路径当前 release 不存在此 md5 文件（指令假定有，属臆断） | 滚动发布库改用"下载时实测 sha256 + 条目数 + 下载日期"绑定身份，并记录 release 版本 | 待用户回传 sha256 与条目数 |
| 2026-09-22 | 段 1 交付：用户按运行命令执行，`cd ~/yeast_test/sop/...` 不存在、文件从未落盘 | **交付规程缺失**：只给了运行命令，没给目录创建与文件传输步骤，默认假设了传输通道存在；连带暴露 settings 的 genome 行用相对路径会在包目录下指错 | settings 改服务器绝对路径；README 明确产物目录；生成 `deploy_to_server.sh`（heredoc + sha256 自校验）+ 短校验命令块；规程入 D-021 | 待用户落盘后 `--check` 复测 |
| 2026-09-22 | 段 1 第 4 阶段失败：`RM_out 下未找到 consensi.fa.classified`（服务器实跑）；实际 RM 5:33 成功（22 families），产物落在**包根目录** | driver 臆造 `-dir`（RM 2.0.7 无此选项）+ subprocess 未设 cwd——DB 与 RM_* 目录全落在启动目录；教训：**工具 CLI 选项必须按目标版本 -h 实测核对，凭记忆拼参数即此类事故** | run() 加 cwd；RM 调用删 `-dir`；collect 改 glob workdir/RM_*；本次 RM 成果归档不删（SC288C-families 等仍有价值） | 待用户归档后重跑复测 |
| 2026-09-22 | RepeatClassifier 警告"curated TE consensus library 不完整"（本机 Dfam 39 仅 full 分区） | Dfam 安装只含 `dfam39_full.0.h5`，缺 curated 分区文件（Siganus 机为 dfam40 + curated 两件套） | 记录观察不夸大：本流程用 `-lib` 自定义库屏蔽，质量不依赖 Dfam 分类；分类家族名可信度下降留作后续完善项（与 PIT-001 关联） | — || 2026-09-22 | prefetch/fasterq-dump 报 `Can't locate XML/LibXML.pm`，@INC 全是 braker3 的 perl5 路径；且兜底 bzip 未触发、四段 gzip/mv 全链报错 | 双因：① base 壳导出的 PERL5LIB 污染（braker3）使 sra_tools 的 perl wrapper 找不到自带模块——与 AGAT/busco 同一个"env 工具须用 env 解释器+清污染"模式；② 会话指令 `cmd \| tail \|\| {fallback}` 再次取到 tail 的退出码 0，fallback 被吞（error_log 已有一条同名教训，未遵守） | 下载改走 ENA 直连 fastq（免 sra-tools）；备选 NCBI S3 .sra + conda activate sra_tools + unset PERL5LIB；指令纪律：**兜底分支不得与管道同用**，写进 SKILL.md | 待用户复测 |
| 2026-09-23 | **严重**：用户执行会话指令 `rm -f WT_Rep*.fq.gz rnaseq.sha256` 后，4 个刚改名落位的 RNA-seq 数据文件（共 2.7 GB）被删除；此前一行的 `gzip -t` 刚证明它们完好 | 指令给出时目标目录已无残留空文件（ll 已显示），该 rm 是"清空上次 wget 404 残留"的过期指令，用户改名后照贴执行；裸 `rm -f` + 通配符对命名冲突零防护，属我方危险指令纪律失败 | 数据恢复 = 本地尚有原件，重新上传并按目标名保存（无服务器侧 rm/mv，改在传输环节完成命名）；教训入 SKILL.md：**禁止在用户指令中给出裸 `rm -f` + 通配符；删除一律先 `ls` 现状、点名到文件、或交由用户自查**；记录 D-022 | 待用户重传后 gzip -t 复测 |
| 2026-09-23 | BRAKER 启动 3 秒即退出：`$AUGUSTUS_SCRIPTS_PATH not set!`（line 1832） | driver 只设了 AUGUSTUS_CONFIG_PATH；BRAKER 猜测 scripts 路径（config/../scripts、/usr/share/augustus/scripts）均不存在，conda augustus 实际把脚本装在 `bin/`（evid：bin 内有 augustus2gbrowse.pl/autoAug.pl）——BRAKER 的猜测逻辑不覆盖该位置，需显式指定 | driver 增加 augustus scripts 目录探测（候选：bin、env/scripts、config/../scripts，按脚本标记文件判定）并设 env+命令行参数；`--resume` 续跑跳过已完成的 protein_prep | 待用户替换文件后续跑复测 |
| 2026-09-23 | BRAKER 二连败（配置期 3 秒）：`$GENEMARK_PATH not set!`——driver 把 `gmes_petap.pl` 文件路径当 GENEMARK_PATH 传，BRAKER 要求**目录**且优先含 `gmetp.pl` 的安装根目录 | 对 BRAKER 3.0.8 的 GENEMARK_PATH 语义理解错误（凭记忆拼参数，无文档核对） | driver 改为目录语义：从 settings genemark_bin 的 gmes 目录向上探测含 gmetp.pl 的根（候选 gdir/parent/grandparent），PATH 与 --GENEMARK_PATH 均用该根目录；教训与"CLI 参数须实测核对"同源，强化 SKILL.md 参数纪律 | 待用户替换后继跑复测 |
| 2026-09-23 | BRAKER 三连败（配置期 3 秒）：`$PROTHINT_PATH not set!`——ETP 模式 + --prot_seq 必需 ProtHint，driver 未提供 | BRAKER 3.0.8 配置期必需路径清单（实测）：AUGUSTUS_CONFIG/BIN/SCRIPTS、PYTHON3、BAMTOOLS、SAMTOOLS、DIAMOND、GENEMARK、PROTHINT——前几次只补齐了前段 | settings3 加 prothint_dir 提示位；driver 探测（rglob prothint.py，回退 braker env）并设 PROTHINT_PATH + --PROTHINT_PATH；配置必需项清单固化进 driver 预检 | 待用户替换两文件后续跑复测 |
| 2026-09-23 | BRAKER ETP 运行 3.5 分钟后失败：gmetp.pl 报全染色体 "does not match any sequence in the fasta file" + GeneMarkS error 256 + 缺 transcripts_merged.fasta.gff；BRAKER 官方提示"证据不足"具误导性 | **PIT-008**：BRAKER 净化器把含空格的 FASTA 头整行用下划线拼接成 ID（NC_001133.9_Saccharomyces_...），而 BAM @SQ 是首字段（NC_001133.9）——ETP 提取转录本找不到序列。Siganus GCA048 v3 同款事故两度实跑 | driver 新增 PIT-008 双防线：预检对照 FASTA 首字段集合 ↔ 全部 BAM @SQ 集合（不一致即拒）；genome_prep 阶段自动生成 id-only 副本（校验顺序/长度/忽略大小写序列/ID 唯一），BRAKER/gffread/export 全链路引用 id-only | 待用户替换两文件后 --resume 复测 |
| 2026-09-23 | BRAKER ETP 蛋白环反复失败（~5 轮）：`gmes_petap.pl` 预测阶段（--predict_with/--mask_penalty/penalty 与 nonhc 环）在其随机命名 scratch 目录内期待 `prothint/prothint.gff`（gmes_petap.pl:223 ResolvePath 相对 $work_dir），训练阶段的 ProtHint 产物不随行；三路软链（penalty/、nonhc/、$wd/）均无法覆盖随机 scratch | git 版 GeneMark-ETP 的版本缺陷：预测与训练的 hints 传递断裂（官方流程中 gmes_petap 应自跑 ProtHint 于 scratch，line 281-287 的自检在预测路径未生效） | **Plan B 换轨**：settings3 `use_protein_evidence=false` → BRAKER ET 模式（GeneMark-ES + RNA 证据，绕开蛋白环）；driver 加开关 + GeneMark-ET* 目录自动探测 + provenance 如实记录降级原因。ETP 全链发现（PIT-008、0 字节复用、ProtHint 接线）保留入档，供后续修复 ETP 后回归 | 待 ET 模式跑通复测 |
| 2026-09-24 | AGAT 报 `Unknown option: g`——driver 用了单杠 `-gff=path` 形态 | 该版 AGAT（Getopt 配置）不认单杠多字符加等号，usage 实测为 `--gff/-f`；参数纪律（以目标版本 --help 为准）执行到位的案例 | driver 改 `--gff <path> -o <path>` 空格形态 | 待用户续跑复测 |
| 2026-09-24 | AGAT 后 gffread 裸 FileNotFoundError：driver D 阶段误用 `braker_env/bin/gffread`（该 env 无此件），settings3 的 `gffread` 键（正确路径 .conda/braker3/bin）未被引用 | 变量引用笔误 + run_env 缺 OSError 兜底导致 traceback 裸奔 | 改用 `s["gffread"]`；run_env 捕获 OSError 转 RuntimeError 友好报错 | 待用户续跑复测 |
| 2026-09-24 | export_coding_gff3.py 报 `SC288C.intron08.longest.gff3` 不存在 | Siganus 布局约定残留：driver 让 AGAT 输出 `SC288C.longest.gff3`，却按旧 prefix 让 export 找 `SC288C.intron08.longest.gff3`——两处命名未对齐 | settings3 `export_prefix` 改为 `SC288C.longest`（对齐 AGAT 实际产物名，export 输出走 coding_only_verified/ 子目录不冲突）；教训：SOP 生成时同文件链的命名必须单一来源 | 待用户续跑复测 |
| 2026-09-24 | 段 3 收官：TSEBRA 合并后 221 基因（BUSCO 3.1%），单因素对照（去 --filter_single_exon_genes）→ 5,384 基因 + BUSCO 99.0% | **PIT-009**：单外显子过滤器物种依赖——酵母内含子贫乏（~5% 基因含内含子），过滤器滤掉真基因主体；Siganus 的"正确配置"跨物种即灾难（PIT-002 同类方法论） | 基线机制真实拦截 → 对照实验定位 → settings 化开关（filter_single_exon_genes=false）+ driver 内嵌 BUSCO fail-fast（busco_min_complete_pct=95） | 复测 = 本交付集（5,384/BUSCO 99.0%）达标 |
| 2026-09-24 | 会话中三次手打命令路径/扩展名错误（gffread 路径错、目录基准错、.gtf 写成 .gff×2） | 临时命令未遵守"以 settings/文档记录值为唯一来源"纪律，凭记忆手打 | 纪律固化：临时命令一律从 settings3.json/deploy 内容复制关键路径；error_log 记满三次 | — |
| 2026-09-24 | 段 4 [B] best_hits 写出时 `Unknown format code 'f' for object of type 'str'`（NR 库 28 分钟后崩） | parse_diamond_hits 只转了 bitscore/evalue/qcovhsp 三个数值列，scovhsp 未转 float；报错行是物理行(两表达式同列)，初看误指 qcovhsp | 补 `row[\"scovhsp\"] = float(...)`；教训：17 列全数值列一次性统一转换，别逐列手补 | 待用户换文件后 --resume（NR 库将重跑，~28min 一次性代价） |
| 2026-09-24 | 段 4 GO 蛋白数=0（IPR 4,984 正常） | InterProScan 5.76 TSV 的 GO 列是多值分隔（GO:xxxx|GO:yyyy），解析用了 fullmatch 整格匹配 → 多值必然失败；IPR 列单值幸免 | 改为 re.findall 逐 cell 提取（内容识别、多值兼容，呼应 PIT-003 列号教训）；D/E 两处解析统一 | 待用户归档 GO=0 期产物后 resume |
